import React, { useEffect } from 'react'

// Empty for now
const Dashboard = () => {
    useEffect(() => {
        window.scrollTo(0, 0)
    }, [])

    return (
        <div>
            User Dashboard
        </div>
    )
}

export default Dashboard
