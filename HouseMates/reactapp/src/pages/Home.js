import React, { useEffect } from 'react'

// Empty for now
const Home = () => {
    useEffect(() => {
        window.scrollTo(0, 0)
    }, [])

    return (
        <div>
            Website Homepage
        </div>
    )
}

export default Home
